import 'package:flutter/material.dart';

class Pallets {
  Pallets._();

  static const Color bgColor = Color(0xFF222121);
  static const Color accentColor = Color(0xFFFDC266);

  static const LinearGradient appGradient = LinearGradient(colors: [
    Color(0xFF8EDDD8),
    Color(0xFFFFC163),
  ]);
}
